# TrackSense AI — backend dependencies (all versions pinned)
#
# Target: Python 3.11, CPU only. No GPU required.
# On Windows the PyPI `torch` wheel is already the CPU-only build.
# On Linux, if you want to avoid the large CUDA payload, install torch with:
#   pip install torch==2.5.1 --index-url https://download.pytorch.org/whl/cpu
# ...then run `pip install -r requirements.txt` for the rest.

# --- Web layer ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20      # required for multipart/form-data image + video uploads
pydantic==2.10.4

# --- Database ---
SQLAlchemy==2.0.36

# --- Imaging / classical computer vision (pipeline step 2) ---
numpy==2.1.3
opencv-python-headless==4.10.0.84
Pillow==11.0.0

# --- Hugging Face (pipeline step 1) ---
transformers==4.47.1
torch==2.5.1
safetensors==0.4.5
huggingface-hub==0.27.0

# --- Config ---
python-dotenv==1.0.1
